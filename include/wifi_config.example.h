#pragma once

// Copy this file to wifi_config.h and fill in your network's credentials.
// wifi_config.h is gitignored so real credentials never get committed.
#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
